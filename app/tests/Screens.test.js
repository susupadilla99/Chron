import React from 'react';
import renderer from 'react-test-renderer';

//import RegisterSuccessScreen from '../screens/RegisterSuccessScreen'

test('Welcome Screen',  () => {
  //const welcome = renderer.create(<RegisterSuccessScreen/>).toJSON();
  //console.log(welcome);
});