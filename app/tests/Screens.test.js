import React from 'react';
import renderer from 'react-test-renderer';

//import WelcomeScreen from '../screens/WelcomeScreen'

test('Welcome Screen',  () => {
  // Commented out due to jest errors  
  
  // const welcome = renderer.create(<WelcomeScreen/>).toJSON();
  // console.log(welcome);
  
});